Generate a README.md for a mobile web-based lesson plan. Add places for screenshots
Here is info about it:
scrapes and processes data off the school website using node.js every hour,
has its ui built with react and MUI,
you can click on any teacher, room or class to redirect to its plan,
dynamic url routing,
its a Single Page Application,
user can hide unnecesary columns,
highlight current lesson,
title of the webiste changes when you change the plan,
its built with typescript,
uses vite,
uses docker,
is available from a single IP address.

All above is not ordered, please put it in appropriete sections like: technologies used, features.

Include instructions below.`

To run this project locally for production you need to 'docker-compose up'
To run this project for development you need to:
serve website for scraping on port 3000,
in backend/ run 'npm start' and then 'python ./serve_output.py',
in frontend run 'npm dev'
